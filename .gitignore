node_modules/
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*



# Environment files
.env
.env.*

# Whatsapp Files
.wwebjs_cache/
.wwebjs_auth/
whatsapp-session/

# Windows system files
desktop.ini
Thumbs.db


# Scratch/test files — local experiments only, never the committed suite
scratch_test_assign.ts
*.scratch.ts
*.test.local.ts

# Test output, not test source
coverage/
.vitest/
test-results/

# NOTE: the committed test suite lives in tests/ and MUST stay in Git.
# `src/__tests__/` was previously ignored, which silently dropped the whole
# suite from the repository — CI and other developers had nothing to run.
# Ignore test OUTPUT and local scratch files, never test SOURCE.

